
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/onoe

    username: root
    password: mariadb

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true
jwt:
  issuer: sunkyoung.dev@gmail.com
  secret_key: study-springboot
  token:
